import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'layout-be220',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
